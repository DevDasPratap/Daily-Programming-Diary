#include<bits/stdc++.h>

using namespace std;

int main(){
    //Sorting string
    string s;
    getline(cin, s);
    sort(s.begin(), s.end());
    cout<<"Sorting string: "<<s<<endl;
    return 0;
}
